import React from "react";

//IMPORT COMPONENTS

import { MainWrapper } from "./SearchResults.styles";

const SearchResults = ({ searchResults }) => {
  return <MainWrapper>results</MainWrapper>;
};

export default SearchResults;
